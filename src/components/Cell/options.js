/**
 * 基本配置
 * 2018-04-20
 * by watasi
 */

// 返回HeadCell的配置
export const headOption = (options) => {
	return {
	  showMenu: options.showMenu,
	  transparent: options.transparent,
		data: options.data
	}
}